# pypassgen
a password generator written in python that saves passwords to a file and encrypts them with aes-128-cbc (fernet) encryption
this is one of my beginner projects as im still learning things

the old src was my first attempt at some kind of security, but it just encodes the passwords with base64 which isnt safe at all...
